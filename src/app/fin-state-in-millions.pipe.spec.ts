import { FinStateInMillionsPipe } from './fin-state-in-millions.pipe';

describe('FinStateInMillionsPipe', () => {
  it('create an instance', () => {
    const pipe = new FinStateInMillionsPipe();
    expect(pipe).toBeTruthy();
  });
});
